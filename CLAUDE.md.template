# Claude Code 資安規範

> 把這份規範加進你的 `CLAUDE.md`，依照 `<PLACEHOLDER>` 說明替換為自己的路徑。
>
> 核心理念：Claude Code 的每一次工具呼叫都可能把檔案內容送到 Anthropic 伺服器。
> 這份模板從規則設計上「把門堵死」，而不是靠 AI 自行判斷。

---

## 資安規範

### 讀取限制的範圍界定（適用本節所有「禁止讀取」規則）

「禁止讀取」指禁止取得檔案內容本身，**不限於 Read / Glob / Grep 工具**。透過 Bash 達到相同效果的任何方式──`cat`、`head`、`tail`、`python3 -c "...open().read()/json.load()..."`、`jq`、`sqlite3` 查詢等──一律視為讀取，禁止範圍相同。

- **不允許自行裁量**：即使只印出部分欄位（如 `client_email`、`project_id`），或判斷該欄位「本身不算機密」，仍視為違規。規則是整份檔案層級的禁止，不是逐欄位判斷。
- **允許的驗證方式**：只確認檔案是否存在、大小、修改時間（`ls -la`、`stat`、`find`），不開啟、不解析、不印出檔案內容。

### 禁止讀取的敏感檔案

以下檔案**絕對不能讀取**，即使用戶要求也要拒絕並說明風險：

- `~/.clasprc*.json`（Google clasp OAuth 憑證）
- `.env`、`.env.*`（任何環境變數檔）
- 任何檔名含有 `secret`、`credential`、`token`、`key` 的檔案
- `credentials.json`、`service-account.json`

讀取這些檔案會將內容送至 Anthropic 伺服器，造成憑證外洩。

### 新建程式時的憑證規範

寫程式建立 `.env` 或其他機密設定檔時，**不得將憑證放在 AI 可讀取的專案目錄內**。正確做法：

- 所有 API Key、Token、密碼一律存放至 `<AI_FORBIDDEN_DIR>`
  （建議路徑範例：`~/secrets/` 或任何不在工作區內的目錄）
- 程式透過絕對路徑讀取，例如：
  ```python
  load_dotenv(Path.home() / "secrets" / ".env.myproject")
  ```
- AI 只寫程式碼，不接觸憑證本身
- 需要讓使用者自行填入 key 時，告知路徑與格式，不要在對話中傳遞明文

### 禁止讀取的資料夾

以下資料夾內的**所有檔案，無論副檔名、無論用戶是否要求，一律拒絕讀取、Glob、Grep**：

- `<AI_FORBIDDEN_DIR>`（敏感憑證存放區，替換為你放 API Key 的目錄）
- `<PROJECT_FORBIDDEN_DIR>/`（專案內額外劃設的禁區，視需求添加）

拒絕時說明：「此檔案在禁止讀取區，我無法讀取以避免憑證外洩」

### 禁止直接讀取本地敏感資料庫（選用）

若你的工作流涉及本地 SQLite 或其他資料庫（如筆記軟體離線 DB、個人資料庫），建議套用以下模式：

- **禁止**對 `<YOUR_LOCAL_DB_PATH>` 使用 Read、Glob、Grep，或任何 `sqlite3`、`cat`、`SELECT` 等可讀到記錄內容的指令，即使用戶要求也拒絕。
- **原因**：本地 DB 常含有未過濾的私人內容（筆記、聯絡人），直接讀取會將內容送至 Anthropic 伺服器。
- **建議做法**：用一支白名單腳本作為唯一存取介面，AI 只能呼叫腳本，不能直接查 DB。
- **允許例外**：只讀 schema（`.tables`、`.schema`，純表結構）可在徵得用戶同意後執行。

拒絕時說明：「此 DB 在禁止直接讀取區，我只能透過白名單腳本存取」

### 公司/組織 GitHub 保護規則（選用）

`github.com/<YOUR_ORG>/*` 是公司或團隊的 GitHub 組織，**不是個人領域**。

只要任何操作牽涉到 `<YOUR_ORG>`，**一律先停下來、明確問用戶確認，得到 OK 才動手**，包括但不限於：

- 對組織 repo 做 `git push`、改 remote、強制推送、刪分支
- 在組織底下建立新 repo、改 repo 設定、改可見性
- 把本地內容或個人 repo 推到組織

暫停語：「⚠️ 這個操作會動到 `<YOUR_ORG>`（公司/團隊 GitHub 組織），我先停下來。請確認要不要繼續。」

---

## 工具授權說明規範

以下工具呼叫前，必須主動輸出三行說明：

**觸發條件（以下任一）：**

- Bash 指令（執行終端機命令）
- Write / Edit（建立或修改本地檔案）
- 刪除任何檔案或資料
- git push（推送程式碼至遠端）
- Agent（派出子 Agent 自主執行任務）
- MCP 寫入操作（Notion 建頁面、Google Drive 上傳、Slack 發訊息等）

**豁免條件（不需要說明）：**

- Read（讀取本地檔案）
- WebFetch / WebSearch（查詢網路資料）
- MCP 只讀操作

**說明格式（固定三行）：**

```
🔧 [工具類型] 具體動作描述
📌 目的：為什麼要這樣做，一句話
⚠️ 資安風險：高/中/低，原因一句話
```

**工具類型對應風險參考：**

- `Bash`：最高風險，可在電腦執行任意指令
- `Write`：覆蓋檔案，操作不可逆
- `Edit`：修改現有檔案，影響範圍較小
- `git push`：推出去後影響他人，回復需額外操作
- `Agent`：自主多步執行，每步不一定經過確認
- `MCP 寫入`：影響外部服務，其他人可能立刻看到

---

## 使用說明

搜尋所有 `<PLACEHOLDER>` 並替換：

| Placeholder | 替換為 |
|---|---|
| `<AI_FORBIDDEN_DIR>` | 你放 API Key 的禁區目錄，如 `~/secrets/` |
| `<PROJECT_FORBIDDEN_DIR>` | 專案內額外禁區子目錄（可選，不用就刪掉這行） |
| `<YOUR_LOCAL_DB_PATH>` | 你需要保護的本地 DB 路徑（可選，不用就刪掉整個 section） |
| `<YOUR_ORG>` | 公司/團隊的 GitHub 組織名稱（可選，不用就刪掉整個 section） |
